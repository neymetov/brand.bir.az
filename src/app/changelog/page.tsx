// ОБЩИЙ changelog для всех брендов/компонентов — не разбит по разделам
// верхнего уровня (§3.1). Источник версий/механизм передачи в Strapi — §3.6,
// открытый вопрос №2 в docs/PROMPT.md §7, требует решения перед реализацией.
export default function ChangelogPage() {
  return (
    <main>
      <h1>Changelog</h1>
    </main>
  );
}
