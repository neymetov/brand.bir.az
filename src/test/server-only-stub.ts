// Пустышка вместо пакета `server-only` (см. vitest.config.ts): сам пакет
// намеренно падает при импорте из клиентского бандла, а в тестах серверный
// код исполняется в Node, и падать нечему.
export {};
