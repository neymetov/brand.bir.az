# Что не нужно в сборке на Vercel. Здесь только то, на что НЕ ссылается код:
# strapi-schemas исключать нельзя — populate.ts импортирует схемы компонентов
# прямо в сборку, и без них приложение не соберётся.

# Strapi живёт отдельным сервисом и своим репозиторием — Vercel собирает
# только Next.js-приложение из корня.
cms

# Разбор качества и журнал решений: нужны в работе, не в сборке.
_qa
docs

# Локальные инструменты разработки.
.husky
docker-compose.yml
.DS_Store
